<% if @group.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@group.errors.count, "error") %> prohibited this clique from being saved:</h2>

    <ul>
    <% @group.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= semantic_form_for(@group) do |f| %>
  <%= f.inputs :name %>
  <%= f.buttons do %>
    <%= f.commit_button "Create Clique" %>
  <% end %>
<% end %>
